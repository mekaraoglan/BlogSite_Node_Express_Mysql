<!DOCTYPE html>
<html lang="tr">

<head>
    <%- include("../partials/head") %>

</head>

<body>

    <%- include("../partials/header") %>

        <div class="container" style="margin-top: 120px">
            <h2 class="mb-4">User List</h2>
            <div class="table-responsive">
                <table class="table table-bordered table-striped">
                    <thead class="table-dark">
                        <tr>
                            <th>User Name</th>
                            <th class="text-end" style="width: 200px">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% users.forEach(user=> { %>
                            <tr>
                                <td>
                                    <%= user.fullname %>
                                </td>
                                <td class="text-end">
                                    <a href="/admin/user-edit/<%= user.id %>"><button style="width: 60px;"
                                            class="btn btn-primary btn-sm me-2">Edit</button></a>
                                    <form action="/admin/user-delete/<%= user.id %>" method="POST" class="d-inline"
                                        onsubmit="return confirm('Are you sure you want to delete this user?');">
                                        <button style="width: 60px;" type="submit"
                                            class="btn btn-danger btn-sm">Delete</button>
                                    </form>
                                </td>
                            </tr>
                            <% }) %>
                    </tbody>
                </table>
            </div>
        </div>

        <%- include("../partials/footer") %>

            <%- include("../partials/script") %>

</body>

</html>